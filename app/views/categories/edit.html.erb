<h1 class="text-center mt-4">Edit Category name</h1>

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-10">
              <%= render 'form' %>  
            </div>
        
            <%= link_to '[ Cancel and return to category ]', category_path(@category), class: "remove-line text-center mt-2 text-info" %>
        </div>
</div>